# Ambiente de desenvolvimento: tudo, os dois lados e a bancada.
# A imagem do gateway NAO usa este arquivo -- ver requirements-gateway.txt.

# gateway
-r requirements-gateway.txt

# device
sounddevice
numpy
webrtcvad
# webrtcvad ainda importa pkg_resources, removido no setuptools 81
setuptools<81
# palavra de ativacao (D30). Roda o modelo por onnxruntime, que o Piper ja
# traz -- nao entra no requirements-gateway: la nao ha microfone (D13).
openwakeword

# dev
pytest
pytest-asyncio

# lab (bancada de testes; nao vai para producao)
faster-whisper
torch          # com CUDA: --index-url https://download.pytorch.org/whl/cu124
torchaudio
transformers
piper-tts
vosk
edge-tts      # so como referencia de qualidade; nao e candidato
soundfile
kokoro
speechbrain
piper-tts[train]   # extras de treino: lightning etc (fine-tune de voz)
numba              # monotonic align: a wheel do piper no Windows vem sem a extensao
